import React from 'react';
import ReactDOM from 'react-dom/client';

function Palette(): React.JSX.Element {
  return <div>Palette coming soon.</div>;
}

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Palette />
  </React.StrictMode>
);
